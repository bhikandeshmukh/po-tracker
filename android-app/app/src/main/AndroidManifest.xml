<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POTracker">
        
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.POTracker.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.MainActivity" android:theme="@style/Theme.POTracker.NoActionBar" />
        <activity android:name=".ui.PODetailActivity" android:theme="@style/Theme.POTracker.NoActionBar" />
        <activity android:name=".ui.ShipmentDetailActivity" android:theme="@style/Theme.POTracker.NoActionBar" />
        <activity android:name=".ui.AppointmentDetailActivity" android:theme="@style/Theme.POTracker.NoActionBar" />
        
    </application>
</manifest>
